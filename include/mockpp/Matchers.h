#ifndef  MOCKPP_MATCHERS_HEADER_GUARD
# define MOCKPP_MATCHERS_HEADER_GUARD

#include <functional>

namespace mockpp {

    /**
     * Ignore is an empty tag structure used to specify that an argument is to
     * be ignored.
     */
    struct Ignore
    {
    };
}

#endif //MOCKPP_MATCHERS_HEADER_GUARD
